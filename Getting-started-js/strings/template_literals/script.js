// template literals

const greeting = `Hello Witch`;
console.log(greeting);


/// string interploation

const username = 'Darlin...'
const welcomeMessage = `HEllO, ${username}! welcome to my page`;
 
console.log(welcomeMessage)


//multiline string

const poem =`roses are red,
violet are blue,
Sugar is sweet ,
And so are you?`

console.log(poem);

// expression evaluation

const price = 10;
const tax = 0.05;
const total =`The price: ${price * (1 + tax)}`;

// escaping backticks

const stringWithBacktick = `he's a backtick \``;